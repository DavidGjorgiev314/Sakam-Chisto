apiVersion: v1
kind: Service
metadata:
  name: frontend
  namespace: sakamchisto
  labels:
    app.kubernetes.io/name: frontend
    app.kubernetes.io/part-of: sakamchisto
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: frontend
  ports:
    - name: http
      port: 80
      targetPort: http
