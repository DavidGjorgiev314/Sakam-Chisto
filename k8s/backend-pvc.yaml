apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: backend-uploads
  namespace: sakamchisto
  labels:
    app.kubernetes.io/name: backend
    app.kubernetes.io/part-of: sakamchisto
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
