apiVersion: v1
kind: Service
metadata:
  name: backend
  namespace: sakamchisto
  labels:
    app.kubernetes.io/name: backend
    app.kubernetes.io/part-of: sakamchisto
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: backend
  ports:
    - name: http
      port: 8080
      targetPort: http
